{
  "mcpServers": {
    "xamp": {
      "command": "uv",
      "args": [
        "run",
        "--directory",
        "/path/to/mcp-xamp",
        "mcp-xamp"
      ],
      "env": {
        "MCP_XAMP_ALLOW_WRITE": "true"
      }
    }
  }
}
